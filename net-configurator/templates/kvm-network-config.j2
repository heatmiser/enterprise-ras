# {{ ansible_managed }}
# cloud-init network-config v2 — management NIC only (enp1s0, 1st virtio NIC).
# Data NICs (enp2s0+) are intentionally unconfigured here; nmstatectl sets them up.
version: 2
ethernets:
  enp1s0:
    dhcp4: false
    dhcp6: false
    addresses:
      - "{{ _mgmt_ip }}"
    gateway4: "{{ _mgmt_gw }}"
    nameservers:
      addresses:
        - 8.8.8.8
