# SPDX-FileCopyrightText: Copyright (c) 2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved.
# SPDX-License-Identifier: MIT

# Runtime dependencies for ERA automation scripts.
#
# Upper bounds prevent major-version surprises on first install. Bump
# deliberately (and re-run the test suite + all three `make generate`
# arches) when raising a ceiling.
ansible>=13.5.0,<14
ansible-core>=2.20.4,<2.21
httpx>=0.27.0,<0.29       # Air API HTTP client (scripts/airlib/)
openpyxl>=3.1.0,<4        # Excel file parsing (scripts/excel_parser.py, topology_generator.py)
pyyaml>=6.0,<7            # YAML reading/writing
rich>=13.0.0,<15          # Console output for Air scripts
setuptools>=70.0.0        # Restores distutils compatibility for Python 3.12 (ansible.posix.firewalld)
jinja2>=3.0.0,<4          # Transitive dep of ansible-core; pinned here to keep the test suite's direct import (tests/test_templates.py) on a matched version

